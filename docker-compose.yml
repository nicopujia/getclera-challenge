services:
  surrealdb:
    image: surrealdb/surrealdb:v2
    command: start --user root --pass root --bind 0.0.0.0:8000 file:/data/srdb.db
    ports:
      - "8000:8000"
    volumes:
      - surrealdb-data:/data

volumes:
  surrealdb-data:
